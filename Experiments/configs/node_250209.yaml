# model configs
model: node
input_dim: 4
meta_dim: 4
hidden_dim: 256

# train configs
epochs: 4000
batch_size: 512
lr: 0.001
wd: 0.0001
seq_len: 24
pred_steps: 24  # number of prediction steps during training
curriculum: True  # use curriculum learning